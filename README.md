# tc03-cloud-mlops

Repositório inicial para o trabalho de Cloud MLOps.

Instruções rápidas:

- Certifique-se de ter uma chave SSH configurada para GitHub.
- Para enviar alterações iniciais:

```bash
git checkout -b main
git add .
git commit -m "Initial commit"
git remote add origin git@github.com:willmoreira7/tc03-cloud-mlops.git
git push -u origin main
```
